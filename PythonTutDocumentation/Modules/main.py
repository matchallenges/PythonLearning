#import branch as b

#from branch import add as a

#print(a(1, 2, 3, 4))

#print(dir(a))

# dir() lists all types of names: variables, modules, functions, etc.

import branch

# the branch code won't execute the print statement because it's name is not __main__ it's name becomes branch
